{
  "name": "Couchbase_Transport_Plugin_for_Elasticsearch",
  "private": true,
  "scripts": {
    "pull:capi:server": "echo \"--Pulling capi-server\" &&  cd couchbase-capi-server && git pull",
    "pull": "echo \"--Pull1ing origin\" && git pull && echo \"--Merging chages from upstream\" && git fetch upstream && git merge upstream/master",
    "pull:all": "npm run pull:capi:server && npm run pull",
    "build:capi:server": "cd couchbase-capi-server && mvn clean install -Dmaven.test.skip=true",
    "build": "npm run build:capi:server && mvn clean install"
 }
}
